# /ship review log: skill-shelf v0.1.0

ship-review round 1: 18 confirmed / 17 fixed / 1 deferred-to-gate, per lens: correctness 6, security 4, test-coverage 3, simplification 1, plan-conformance 4 (7 raised findings refuted by skeptics; 1 verifier lost to API error — its finding folded into the sync-test-coverage cluster). Dedup: 18 confirmed ≈ 11 distinct issues, all fixed in one agent round (25→38 tests, mutation-verified). Notable: HIGH org-plugin-names-in-committed-constant — also found in the committed (unpushed) plan; plan scrubbed and commit amended (c4e8382 → bb1bdb4) before any push. "Task 3 real run + recovery mechanism absent" is the designed human gate, not a defect.
ship-review round 2: 3 confirmed / 3 fixed / 0 left, per lens: correctness 3 (of 7 raised; 4 refuted). All second-order defects of round-1 fixes: liveness refusal misdirected to array index (now 1-based file line numbers), root-conservation still tautological (check REMOVED with rationale in code+plan — anti-bloat per user directive), rawLines default silently degraded the gate (now a hard refusal). Fixed by orchestrator inline (38→37 tests: tautology test deleted).
ship-review round 3 (Stage-6 spot-review of the orchestrator patch): 1 confirmed / 1 fixed / 0 left — sync success line still advertised the removed check (false coverage in operator output); fixed, plus free hardening: liveness probe now uses bare line text (filler-substring hazard eliminated). Verified: lineNo math correct for LF+CRLF (empirical), no vacuous tests, 37/37 green, build green.
review-loop: complete

Task 6 codex milestone (2026-08-16): cross-vendor review of the Astro
implementation by the skill-shelf-codex spoke. 4 findings: 1 medium
(count-up digits existed only as CSS pseudo-content — fixed with real
sr-only digits + aria-hidden animated counter, verified in dist HTML),
2 low fixed (glob-extension mismatch now warns at build; dead
--color-accent-deep token and duplicated .project .notes rule removed),
1 low refuted (favicon oklch literal — a data URI cannot reference CSS
custom properties; documented as a reviewed platform-limitation exception
with a change-both-together comment). 60/60 tests, build green, dist
still has zero script tags after fixes.

Task 3 gate (2026-08-16): user reviewed the full entry list twice (HTML + raw JSON). Gate decisions: (1) generated.json carries ONLY personal entries — marketplace scanned but never published; (2) the 12 skills installed from mattpocock/skills collapse into one matt-pocock-skills aggregate (sync-level AGGREGATE_GROUPS, membership verified against the upstream repo listing + ecosystem markers); (3) denylist loss-recovery WAIVED by owner — machine-local only, terms gitignored at any depth as belt-and-braces. v2 data approved; 15 entries written and committed. Stability check + org smoke green.
