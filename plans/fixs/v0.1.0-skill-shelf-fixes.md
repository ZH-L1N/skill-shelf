# Fixes log: skill-shelf v0.1.0

- Plan: `plans/v0.1.0-skill-shelf.md`
- Started: 2026-08-16T16:20:00-07:00
- Reviewer: Claude Opus 5, xhigh effort (herdr pane wZ:p5 — user-directed
  transport override replacing the OpenAI API / Codex CLI paths; reviewer had
  read-only filesystem access and verified findings empirically)
- Planner: Claude Fable 5 (hub, wZ:p1)
- Termination rules: NO FINDINGS | planner-locked | ceiling (20) | severity-gated

> Redaction note: this file is committed to a PUBLIC repo. Org-internal
> product names quoted by the reviewer in finding 2 are replaced with
> `[REDACTED-ORG-TERM]`. The unredacted round artifact lives locally at
> `~/.local/state/herdr-orch/ss-plan-review-r1/result.md` (never committed).

## Round 1 — 2026-08-16T16:18-07:00

### Reviewer findings (Claude Opus 5 pane)

1. **[HIGH]** [silent-data-loss-symlinks] The sync script discovers zero
   skills from `~/.agents/skills`, silently dropping an entire declared
   runtime. All three entries there are symlinks, and `fs.globSync` does not
   descend into symlinked directories. Verified on the exact Node the plan
   pins (v24.15.0): `fs.globSync('~/.agents/skills/*/SKILL.md')` returns
   `[]`. A readdir+statSync scan of the same roots finds 42 SKILL.md files;
   the plan-as-written finds 39. The plan's own research doc (§3.1) carries
   the warning ('followSymlinks needs Node >=24.16.0; local is 24.15.0') and
   the plan dropped it without substituting a symlink-safe strategy. Nothing
   in the plan can detect the loss: unit tests use synthetic fixtures; the
   Task 3 human gate surfaces wrong entries but not absent ones; every
   verification check passes on a silently truncated inventory.
   *Concrete fix:* readdirSync + statSync discovery (statSync follows
   symlinks); fail-loud post-scan assertion (every source root >= 1 entry,
   exit non-zero naming empty roots); pinned test 'symlinked skill directory
   is discovered' with a fixture symlink.

2. **[HIGH]** [org-leak-defense-unverifiable] The org-leak smoke test cannot
   detect the single most likely leak. Verified:
   `~/.claude/skills/offline-read-telemetry-policy` contains zero occurrences
   of the string 'exowatt'; its frontmatter description names internal
   products verbatim ('… [REDACTED-ORG-TERM] edge producer
   ([REDACTED-ORG-TERM], [REDACTED-ORG-TERM], [REDACTED-ORG-TERM]) …'). If
   that slug ever falls out of the hardcoded BLOCKLIST, the description ships
   into the committed, public `data/generated.json`, and
   `grep -ri exowatt data/ dist/` returns no hits. Both lines of defense are
   weaker than claimed: the blocklist test exercises the pure function with
   injected input (asserts nothing about the real constant); the
   entry-by-entry review is explicitly one-time while sync recurs; the smoke
   test is a single literal-string match; marketplace blocking is exact-match
   on one name.
   *Concrete fix:* (a) test the real constant's contents; (b) pattern-match
   marketplaces (`/exowatt/i`); (c) content denylist over the generated
   output covering the org terms that actually appear, run on every sync and
   in CI; (d) make the entry-by-entry review a gate on every sync via a
   diff-summary + confirmation flag.

3. **[HIGH]** [scaffold-command-contradicts-source] Task 1's scaffold command
   omitted the target directory `.` and the `--install --no-git --no-ai
   --skip-houston` flags that the plan's own research doc (§1.3) gives
   verbatim as the verified invocation for a repo that already has git
   history, CLAUDE.md and docs. As written it hangs the autonomous /ship
   pipeline on an interactive prompt, may write AI agent files over the
   repo's safety-bearing CLAUDE.md, and may `git init` inside an existing
   repository — then 'Commit'.
   *Concrete fix:* restore the exact researched command; pre/post sha256
   assertions on CLAUDE.md/README.md; fail if any tracked file deleted; no
   `git add -A`.

4. **[MEDIUM]** [unassigned-requirement] The double-sync zero-diff stability
   check can never pass as specified: `generatedAt` is a wall-clock timestamp
   that changes every run. The verification section states its own fix
   parenthetically but no task owns it; `toStableJson(entries)` takes entries
   alone and cannot decide whether to advance the timestamp.
   *Concrete fix:* `toStableJson(entries, previousJson)` preserves
   `generatedAt` when entries are byte-identical; pinned test; verification
   stated as an exact command.

5. **[MEDIUM]** [ambiguous-merge-tiebreak] When the same slug exists in both
   `~/.claude/skills` and `~/.codex/skills` with different frontmatter, which
   description gets published is undefined ('first non-empty wins' + unpinned
   source order). Live case: 15 slugs in both roots; `proj-init` diverges
   (177 vs 221 chars). The pinned test would encode whatever the
   implementation happens to do.
   *Concrete fix:* declared ordered constant `claude > codex > agents`
   consumed by `mergeEntries`; pinned test using the real divergence shape.

6. **[MEDIUM]** [missing-edge-case] A skill directory with no readable
   SKILL.md is unhandled; `~/.codex/skills/codex-primary-runtime` exists and
   is completely empty. Either a ghost entry ships or sync crashes ENOENT in
   the untested CLI layer.
   *Concrete fix:* readable SKILL.md as the inclusion predicate; 'skipped N'
   summary; pinned tests for absent and unreadable SKILL.md.

7. **[MEDIUM]** [content-quality-gap] Frontmatter `description` is LLM
   routing text, not card copy: median 236 chars, p90 445, max 1077 across
   the 76 in-scope SKILL.md files; 46 exceed 200 chars. Rendered on a card
   grid this reads as machine instructions; the minimal-editorial direction
   is undeliverable on real data, and the failure surfaces at the Task 5
   user gate or as a Task 6 mockup-vs-build mismatch.
   *Concrete fix:* Task 4 deliverable — hand-written description override
   (<= 140 chars) for every rendered entry, frontmatter fallback only for
   hidden entries; `mergeCatalog` cap test; before Task 5.

8. **[MEDIUM]** [gitignore-gap] `.scratch/` exists, is untracked, and is not
   gitignored (verified via `git check-ignore` and `git add -A --dry-run`);
   /ship is authorized to push to main and seven of eight tasks end in
   'Commit'. Any `git add -A` publishes orchestration scratch content to a
   public repo.
   *Concrete fix:* gitignore `.scratch/`; explicit pathspecs only; pre-push
   untracked-files assertion.

9. **[LOW]** [stale-source-of-truth] The spec still says
   `public/showcase/` (line 50) and the version-ambiguous cache glob
   (line 62) — both deliberately superseded by the plan's Deviations #1/#2 —
   and Task 8 only reconciles README/CLAUDE.md, never the spec that both
   documents cite as canonical.
   *Concrete fix:* Task 8 amends the spec's two points with supersession
   notes.

OPEN QUESTIONS:
- (oq_r1_1) Is ZH-L1N/skill-shelf actually a public GitHub repository today?
- (oq_r1_2) Which personal entries are intended to be hidden? (union is 27
  after blocklist, not ~25; shelf size undecided going into the design gate)
- (oq_r1_3) Does the `agents` runtime belong on the site at all, as an
  explicit decision rather than a side effect of the symlink bug?

### Planner decisions (Claude)

1. **Accept** — Global constraints gain a symlink-following discovery rule;
   Task 2 discovery spec rewritten to readdirSync+statSync; pinned test #8
   (symlink fixture); Task 3 gains the fail-loud empty-root assertion.
2. **Accept (with one modification)** — All four fixes adopted: real-constant
   test (pinned test #4), `/exowatt/i` marketplace pattern, content denylist,
   every-sync diff gate (sync is dry-run by default, `--write` to apply).
   Modification: the reviewer's fix (c) as stated would commit the org terms
   themselves into the public repo inside a test regex — itself a leak. The
   denylist instead lives in gitignored `data/denylist.local.txt`; committed
   tests verify the mechanism with synthetic terms only; sync refuses to run
   without the local file. CI cannot and must not run sync (unchanged).
3. **Accept** — Task 1 now carries the exact researched command
   (`npm create astro@latest . -- --template minimal --install --no-git
   --no-ai --skip-houston`), sha256 pre/post assertions on CLAUDE.md/README,
   tracked-file-deletion check, and a repo-wide explicit-pathspec rule.
4. **Accept** — `toStableJson(entries, previousJson)` with `generatedAt`
   preservation moved into Task 2 as a deliverable + pinned test #10;
   verification restated as an exact command.
5. **Accept** — `SOURCE_PRECEDENCE = ["claude", "codex", "agents"]` declared
   in the data model and consumed by `mergeEntries`; pinned test #2 encodes
   the real proj-init divergence shape.
6. **Accept** — readable SKILL.md is the inclusion predicate; skipped-count
   summary in sync output; pinned test #9.
7. **Accept** — 140-char hand-written description required for every
   rendered entry (Task 4 deliverable, cap-enforcing test); Task 5 mockup
   explicitly bakes in the final card copy.
8. **Accept** — `.gitignore` += `.scratch/` (and `data/denylist.local.txt`)
   in Task 1; explicit pathspecs codified in Global constraints; pre-push
   untracked-files assertion added to Verification.
9. **Accept** — Task 8 extended to amend the spec's two superseded points.

### Open questions and uncertainty

- (oq_r1_1) resolved by planner as a fact lookup, not a user decision.
- (oq_r1_2), (oq_r1_3) put to the user (batched AskUserQuestion).

### User resolution (verbatim)

> oq_r1_2: "隐藏 2 个工具性条目 (Recommended)" — hide `grill-me` and
> `setup-matt-pocock-skills`; the other 25 personal skills all render.
> (User via AskUserQuestion)

> oq_r1_3: "上站，可能是因为暂时是private，后续有需要可以改成public" —
> include the three `~/.agents` skills, categorized Design & Visual.
> (User via AskUserQuestion. Planner note: the skill-shelf repo itself is
> PUBLIC — verified via `gh repo view` and restated to the user; the
> "private" remark is understood as referring to those skills' own repos,
> so their cards ship without repo links for now.)

- oq_r1_1 → resolved: repo is PUBLIC (`gh repo view ZH-L1N/skill-shelf`
  → visibility PUBLIC, 2026-08-16). Stated explicitly in the plan's Context.
- oq_r1_2 → visible shelf = 25 personal skills; two hidden entries named in
  the plan and the overrides example.
- oq_r1_3 → explicit IN decision recorded as grilling+review decision #2.

### Plan edits applied

- Context: repo visibility stated as PUBLIC with verification date.
- Decisions section: added #2 (shelf size 25, two hidden slugs, agents
  runtime explicitly in) and renumbered.
- Global constraints: org-leak defense rewritten as four implemented+tested
  layers (real-constant test, `/exowatt/i` pattern, gitignored local
  denylist, every-sync diff gate); symlink-following discovery rule added;
  git-hygiene rule (explicit pathspecs, pre-push untracked assertion) added.
- Data model: `SOURCE_PRECEDENCE` tie-break declared; `generatedAt`
  preservation rule declared; 140-char card-copy rule added; overrides
  example updated (grill-me hidden).
- File map: added `data/denylist.local.txt` (gitignored); sync.mjs described
  as dry-run-by-default CLI.
- Task 1: exact scaffold command + hash assertions + .gitignore additions.
- Task 2: discovery rewritten (readdir/statSync); pinned tests expanded from
  6 to 11, including real-constant, symlink, missing-SKILL.md, generatedAt,
  denylist-mechanism cases.
- Task 3: dry-run/--write semantics, denylist refusal, empty-root fail-loud
  assertion, every-sync diff gate.
- Task 4: hand-written <= 140-char descriptions for all rendered entries.
- Task 5: mockup bakes in final card copy from Task 4.
- Task 8: spec amendment (supersession notes) added.
- Verification: exact stability command; source-root assertion; 42-file
  baseline sanity count; pre-push untracked check.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 9 (high=3, medium=5, low=1) + 3 open questions
- Decisions: 9 Accept (1 with modification), 0 Reject, 0 Uncertain;
  2 open questions user-resolved, 1 planner-resolved as fact
- Plan size: grew ~15% (targeted hardening, no scope additions)

### Reviewer raw response

Not embedded: the raw JSON quotes org-internal product names verbatim
(redaction note at top of file). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r1/result.md`.

## Round 2 — 2026-08-16T17:05-07:00

Reviewer re-verified every round-1 fix empirically and hunted for issues the
edits introduced. 10 findings (3 high, 7 medium) + 3 open questions.

### Reviewer findings (Claude Opus 5 pane)

1. **[HIGH]** [new-gate-always-fails] The round-1 fail-loud source assertion
   fires on every run: 2 of 7 manifest plugins have no `skills/` dir at all
   (code-review, swift-lsp) and the two org plugins are blocklisted to zero
   BY DESIGN, so 'every configured root >= 1 entry' can never hold.
   Predictable repair under pressure is deleting the assertion — removing
   the regression net F1 exists to provide.
   *Concrete fix:* assert on raw pre-blocklist counts; register only plugin
   roots that have a `skills/` dir; record expected concrete counts; pinned
   test for the skills-less plugin case.

2. **[HIGH]** [denylist-unverifiable] Layer 2 is dead code in steady state:
   layer 1 drops the org content before the denylist ever sees it, so a
   silently degraded denylist file produces byte-identical output to a
   working one. 'Refuses if missing or empty' is satisfied by a one-character
   file. Gitignoring the file removed version control, review, backup, and
   any second copy — after a machine rebuild it is reconstructed from
   memory, and a reconstruction that omits [REDACTED-ORG-TERM]s while
   remembering one of them passes the check and ships a leak. The matcher
   was also placed in the untested CLI layer while pinned test #11 assumes
   a testable pure function.
   *Concrete fix:* run the denylist pre-blocklist and require it to
   independently flag `offline-read-telemetry-policy` (continuous canary);
   pure `matchesDenylist` in inventory.mjs; store the list at a backed-up
   out-of-repo path or as committed salted hashes; minimum-term-count floor.

3. **[HIGH]** [scaffold-guard-scope] The minimal template ships README.md
   and .gitignore (verified against withastro/astro examples/minimal), so
   in-place scaffolding collides with the repo's existing files: the sha256
   guard fires on README (blocking Task 1 with no stated recovery) and does
   not cover .gitignore at all — whose template replacement silently drops
   the `.env`/`.env.*` exclusions in a PUBLIC repo with an autonomous
   pipeline authorized to push.
   *Concrete fix:* scaffold into a temp dir and move in only src/,
   package.json, tsconfig.json, astro.config.mjs; or extend the guard to all
   tracked files and merge .gitignore by appending.

4. **[MEDIUM]** [fix-reintroduces-original-bug] The F1 fix specifies
   `readdirSync(root, { withFileTypes: true })`; filtering on
   `isDirectory()` — the only reason to want Dirents — silently drops all
   three `~/.agents` symlinks again (verified: isDirectory=false,
   isSymbolicLink=true). Pinned test #8's fixture shape was unspecified;
   a symlink-to-file fixture would pass while reality fails.
   *Concrete fix:* plain `readdirSync(root)`; explicit 'never filter on
   Dirent type' sentence; fixture pinned as symlink→directory-with-SKILL.md.

5. **[MEDIUM]** [count-arithmetic-wrong] '25 visible personal skills' is
   stale: under the F6 readable-SKILL.md rule the union is 27 pre-blocklist
   / 26 post, so 24 visible after hiding two — the round-1 '28/27' counted
   directories including the empty codex-primary-runtime.
   *Concrete fix:* restate as 27/26/24 with codex-primary-runtime named as
   an expected skip; propagate 24 into Tasks 4/5; add expected merged total
   (60 entries, 26 personal) to Verification.

6. **[MEDIUM]** [slug-key-collision] `herdr` is BOTH a real scanned skill
   (claude+codex, SKILL.md verified) and the overrides 'fully manual'
   project entry under the same flat key — the override would mutate the
   scanned skill into a project, silently dropping a skill card and its
   runtimes.
   *Concrete fix:* namespace projects separately (top-level `projects:`
   map) or distinct keys; pinned mergeCatalog test; state whether herdr
   counts toward the visible-skills number.

7. **[MEDIUM]** [verification-false-positive] `grep -ri exowatt data/ dist/`
   now matches the denylist file placed inside `data/` — a permanent false
   positive that trains deletion of the last backstop.
   *Concrete fix:* scope the grep to published artifacts; move the denylist
   out of `data/`.

8. **[MEDIUM]** [wrong-verification-baseline] '42 SKILL.md files … +
   manifest plugins' misattributes the round-1 personal-only measurement and
   compares a file count to a merged entry count. Measured: 84 scanned
   (42 personal + 42 marketplace incl. 8 org), expected merged output 60
   entries (26 personal + 34 marketplace).
   *Concrete fix:* replace with the full reconciliation and have sync print
   it mechanically.

9. **[MEDIUM]** [enforcement-ambiguity] 'mergeCatalog enforces the cap with
   a unit test' doesn't say whether mergeCatalog throws at build time
   (routine skill installs would break the build) or only a fixture test
   asserts it (cap unenforced on real data).
   *Concrete fix:* truncate-at-word-boundary + build warning at runtime;
   committed test over the REAL data files fails npm test on genuine gaps.

10. **[MEDIUM]** [unvalidated-hand-edited-data] Nothing validates overrides
    keys against real slugs: a typo'd `grill_me` silently republishes a
    skill the user chose to hide; stale keys silently no-op.
    *Concrete fix:* orphan-key test over the real files; mergeCatalog
    returns orphanedOverrides, logged at build.

OPEN QUESTIONS:
- (oq_r2_1) Curated Picks undecided but Tasks 4/5 now depend on it — block
  or ship provisional?
- (oq_r2_2) photo-anchored-zine + semantic-zine-distillation share one repo
  (photo-zine-studio) — two skill cards or one Project?
- (oq_r2_3) The stability check runs `--write` twice, mutating the tracked
  data file as a side effect of verifying it — use a temp output path?

### Planner decisions (Claude)

1. **Accept** — assertion moved to raw pre-blocklist counts; skills-less
   plugins skipped at discovery (also recorded in Deviation #1); expected
   raw counts recorded in Task 3; pinned test #12 added.
2. **Accept (with storage choice)** — canary (denylist must independently
   flag `offline-read-telemetry-policy` pre-blocklist every run, else sync
   refuses), pure `matchesDenylist` in inventory.mjs, `DENYLIST_MIN_TERMS =
   5` floor, all adopted. Storage: plaintext at
   `~/.config/skill-shelf/denylist.txt` (out-of-repo, backed-up home config,
   `SKILL_SHELF_DENYLIST` override) chosen over salted hashes — hashing a
   small dictionary of guessable product names adds real complexity for
   near-zero secrecy, and CI never runs sync anyway.
3. **Accept** — temp-dir scaffold adopted (`.astro-tmp`, move four paths,
   delete the rest), CLAUDE.md/README/.gitignore byte-identical assertions
   kept as belt-and-braces, `.astro-tmp/` gitignored.
4. **Accept** — `withFileTypes` dropped; Dirent-filter prohibition written
   into Global constraints and Task 2; fixture shape pinned in test #8.
5. **Accept** — decision #2 restated (27/26/24, codex-primary-runtime named
   as expected skip); 24 propagated to Tasks 4/5; merged-total expectation
   (60 entries / 26 personal) added to Verification.
6. **Accept** — overrides split into `skills:` / `projects:` namespaces;
   projects can never mutate skills; herdr intentionally appears as BOTH a
   skill card (counts in the 24) and a project card; pinned mergeCatalog
   test added.
7. **Accept** — grep scoped to `data/generated.json data/overrides.yaml
   dist/`; denylist moved out of the repo entirely (see decision 2), which
   dissolves the collision.
8. **Accept** — full count reconciliation (84 − 9 → 75 → merge 15 dups →
   60 entries / 26 personal) in Verification; sync prints it.
9. **Accept** — graceful runtime (word-boundary truncation + named build
   warning) + strict data test over the real files.
10. **Accept** — orphan-key test over real files (skills keys must match a
    generated slug or carry `manual: true`); `orphanedOverrides` surfaced
    at build.

### Open questions and uncertainty

- (oq_r2_3) resolved by planner: sync gains `--out <path>`; the stability
  check writes to /tmp twice, diffs the two, then diffs against the tracked
  file — verification never mutates tracked data.
- (oq_r2_1), (oq_r2_2) put to the user (batched AskUserQuestion).

### User resolution (verbatim)

> oq_r2_1: "superpowers 四件套 (Recommended), 但superpowers共用一个卡片就够了吧"
> — the four superpowers skills are the pick, but as ONE shared aggregate
> card, not four cards. (User via AskUserQuestion)

> oq_r2_2: "两张独立的，后续我还会再增加一些生图的skill" — keep two
> independent skill cards; more image-generation skills will join later.
> (User via AskUserQuestion)

- oq_r2_1 → Curated Picks = one manual `superpowers` aggregate card
  (`manual: true` in the skills namespace), naming the four flagship skills
  in its copy. No individual third-party cards. Open item eliminated —
  Tasks 4/5 are unblocked with final numbers (24 + 1 + 1).
- oq_r2_2 → recorded in decision #2; Design & Visual keeps individual cards.

### Plan edits applied

- Deviation #1: skills-less manifest plugins skipped at discovery.
- Global constraints: denylist redesigned (out-of-repo path, canary
  self-test, min-term floor, pure matcher); discovery rule now bans Dirent
  filtering; grep smoke scoped to published artifacts.
- Decisions: #2 restated with measured 27/26/24 and the zine-cards
  confirmation; #3 rewritten as the aggregate Curated Picks card; #4 states
  the herdr dual-card design.
- Data model: overrides split into `skills:`/`projects:` namespaces;
  `manual: true` entries; orphan-key validation; graceful-truncation +
  strict-data-test card-copy enforcement.
- File map: denylist moved out of repo; sync gains `--out`.
- Task 1: temp-dir scaffold with move-list and assertions.
- Task 2: discovery spec rewritten; pinned tests grown to 12 (fixture shape
  for #8 pinned; #11 targets the pure matcher; #12 skills-less plugin).
- Task 3: gate order (floor → canary → output match), raw-count assertion
  with expected numbers, `--out` flag.
- Task 4: two-namespace overrides authoring; 24+1 descriptions; new
  mergeCatalog tests (dual herdr, orphan keys, real-data cap).
- Task 5: mockup card counts finalized (24 + 1 + 1).
- Verification: temp-path stability commands; full count reconciliation;
  site-side card counts.
- Open items: none remain.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 10 (high=3, medium=7, low=0) + 3 open questions
- Decisions: 10 Accept (2 with modifications), 0 Reject, 0 Uncertain;
  2 open questions user-resolved, 1 planner-resolved
- Cumulative findings: 19 raised, 19 accepted, 0 rejected

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r2/result.md`.

## Round 3 — 2026-08-16T17:40-07:00

Reviewer re-verified round 2's fixes (explicitly confirming every round-2
number was re-measured correct) and returned 4 findings (3 medium, 1 low),
zero open questions. Severity trend: 3H+5M+1L → 3H+7M → 3M+1L.

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [untested-integrity-gate] The denylist's integrity checks
   (floor, canary, refuse-to-write) all live in the untested CLI layer;
   only the trivial matcher is unit-tested. Worse, the canary binds sync's
   ability to run to `offline-read-telemetry-policy` staying installed —
   uninstall that work skill and sync refuses forever with a false
   diagnosis. And the canary proves one term matches, not that the list is
   complete: a post-rebuild reconstruction keeping one [REDACTED-ORG-TERM]
   while dropping the others passes both floor and canary.
   *Concrete fix:* term-liveness self-test (every term must flag a
   synthetic record — external-dependency-free); committed non-disclosing
   fingerprint `sha256(sorted(lowercased terms))`; pure `denylistGate` in
   inventory.mjs with pinned tests; skill-presence check demoted to warning.

2. **[MEDIUM]** [verification-breaks-on-correct-code] `--out` (round 2) ×
   `generatedAt` preservation (round 1): the plan never says where
   `previousJson` comes from; under the natural reading both stability runs
   write fresh timestamps to nonexistent /tmp paths and the flagship check
   fails on correct code — intermittently (passes when both runs share a
   clock second). Third consecutive round with a verification step that
   fires on correct code.
   *Concrete fix:* one sentence — previous-state always reads from
   `data/generated.json` regardless of `--out`; pinned test.

3. **[MEDIUM]** [stale-constant-as-assertion] The count reconciliation
   presents point-in-time snapshots (84/9/15/60/26 — all re-verified
   correct today) as mechanical checks; asserting them makes sync fail on
   the first ordinary skill install, the exact event sync exists to handle.
   Same failure shape as round 2's source-root assertion.
   *Concrete fix:* assert the identities (scanned − blocklisted − dups ===
   written; personal + marketplace === written); print the dated figures as
   provenance, never assert them.

4. **[LOW]** [done-definition] 'Open items: None' coexists with two
   placeholders scheduled to ship to a confirmed-public site: the hero bio
   placeholder and the mockup's visible oil-motion slot (Task 6's criterion
   is 'visually matches the mockup').
   *Concrete fix:* launch requirements — final bio text; state whether the
   slot ships as reserved space or is omitted from production markup.

### Planner decisions (Claude)

1. **Accept** — `denylistGate({terms, fingerprint, preBlocklist,
   postBlocklist})` pure in inventory.mjs; term-liveness self-test replaces
   the installed-skill canary (demoted to non-blocking warning);
   `data/denylist.fingerprint` committed (sha256 of the sorted lowercased
   term list — verifying a guess requires reproducing the entire exact set,
   acceptable for guessable-but-unconfirmable product names; a fingerprint
   mismatch refuses and updating the committed value is the explicit review
   moment for term-list changes); pinned tests #13–16.
2. **Accept** — `--out` changes only the write target; previous-state
   always from `data/generated.json`; pinned test #17.
3. **Accept** — identities asserted, snapshots printed as labeled
   2026-08-16 provenance; drift is a human diff-review signal, not a
   failure.
4. **Accept** — Task 8 launch requirements: final bio (collected at the
   Task 5 mockup gate) and NO oil-motion placeholder in production markup
   (hero structured to accept the future animation; visible placeholder is
   mockup-only). Verification list extended accordingly.

### Plan edits applied

- Global constraints layer 2 rewritten around `denylistGate` (floor →
  liveness → fingerprint → output gate; warning-only skill check).
- File map: `data/denylist.fingerprint` added.
- Task 2: `denylistGate` added to pure functions; pinned tests grown to 17.
- Task 3: `--out` semantics pinned; gate order updated; reconciliation
  asserts identities with provenance figures.
- Decision #7 + Task 8 + Verification: bio finalization and
  no-placeholder-in-production launch requirements.
- Status line: rounds 1–3 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 4 (high=0, medium=3, low=1) + 0 open questions
- Decisions: 4 Accept, 0 Reject, 0 Uncertain; no user consultation needed
- Cumulative: 23 findings raised, 23 accepted, 0 rejected

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r3/result.md`.

## Round 4 — 2026-08-16T18:10-07:00

3 findings (2 medium, 1 low), zero open questions. Notably, the reviewer
refuted its OWN round-3 fingerprint recommendation with a working attack.

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [fingerprint-discloses-and-underprotects] The committed
   sha256 fingerprint's 'discloses nothing' claim is false: an unsalted
   digest of a small set of guessable product names is a confirmation
   oracle — demonstrated locally, full term-set recovery from an 11-term
   candidate pool (380 size-4-7 combinations) in under a millisecond, in a
   repo whose CLAUDE.md names the org. It also underprotects: a lossy
   reconstruction and a legitimate edit produce the identical mismatch, and
   the documented remediation (commit the printed value) is the bypass.
   The reviewer flagged this as its own round-3 recommendation, implemented
   faithfully by the plan.
   *Concrete fix (preferred):* drop the fingerprint entirely — the
   backed-up home-config location already covers rebuild drift; delete the
   'discloses nothing' sentence.

2. **[MEDIUM]** [vacuous-assertion] The round-3 count identities cannot
   fail: `personal + marketplace === written` is a tautology over any
   partition, and `scanned − blocklisted − duplicates === written` is
   forced when duplicates are derived from the merge output (verified: it
   'holds' for a merge writing 55, 60, or 71 entries). The designated
   detector for the round-1 failure class detects nothing.
   *Concrete fix:* raw-side checks that can fail — slug set equality
   (names offenders), personal runtime conservation (sum of runtimes ===
   surviving personal instances, 41 today), root conservation
   (filesScanned === sum of per-root counts).

3. **[LOW]** [contradictory-acceptance-criteria] Task 6 'visually matches
   the mockup' vs Task 8 'no oil-motion placeholder box in production'
   contradict on the same element (and in miniature for the bio); an
   implementer satisfying one fails the other.
   *Concrete fix:* Task 6 exclusion clause naming the two mockup-only
   artifacts.

### Planner decisions (Claude)

1. **Accept (preferred option)** — `data/denylist.fingerprint` dropped
   entirely (file map, gate signature, Task 3, pinned tests updated); the
   plan now records WHY no committed digest exists, so it isn't re-added
   later. The committed-term-count alternative was rejected too: it shares
   the detection-equals-bypass weakness and adds nothing over the
   backed-up-location argument.
2. **Accept** — the three raw-side conservation checks replace both
   identities (Verification rewritten; pinned test #18 added; provenance
   figures extended with the runtime-instance sum 41 = 26 + 15).
3. **Accept** — Task 6 now excludes the two documented mockup-only
   artifacts inline.

### Plan edits applied

- Global constraints layer 2: fingerprint bullet replaced by an explicit
  no-committed-digest rationale; gate order is floor → liveness → output.
- File map: fingerprint file removed.
- Task 2: `denylistGate` signature without fingerprint; tests renumbered
  13–16 + new #18.
- Task 3: gate order + stderr guidance cleaned.
- Verification: three raw-side conservation checks with named-offender
  semantics; provenance extended.
- Task 6: mockup-only-artifact exclusion clause.
- Status line: rounds 1–4 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 3 (high=0, medium=2, low=1) + 0 open questions
- Decisions: 3 Accept, 0 Reject, 0 Uncertain
- Cumulative: 26 findings raised, 26 accepted, 0 rejected
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r4/result.md`.

## Round 5 — 2026-08-16T18:45-07:00

2 findings (1 medium, 1 low), zero open questions. (Transport note: from
this round the brief was delivered by plain SendMessage to the reviewer's
live session — the earlier failures were an addressing bug, messages sent
to a zombie session orphaned by the picker incident; injection is retired.)

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [unestablished-premise] The fingerprint was dropped on the
   premise that the term list lives in a "backed-up home-config path" — but
   that coverage is asserted, never established: `~/.config/skill-shelf/`
   doesn't exist yet, `~/.config` is in no git work tree, no dotfiles repo
   exists, and no task sets up any backup. With the fingerprint gone, the
   remaining gate cannot distinguish a correct term list from five
   well-formed wrong terms (per-term liveness only proves each string
   matches a record built from itself). Post-rebuild reconstruction that
   drops a term reports healthy. The reviewer framed this as completing its
   own round-4 recommendation: the argument was conditional ("IF the list
   is restored…") and the plan converted the condition into fact.
   *Concrete fix:* make the premise a step — named recovery mechanism
   (password-manager secure note / Time Machine / private path, NOT a
   dotfiles repo), recorded in CLAUDE.md, verified in the Verification
   list; soften the two prose claims.

2. **[LOW]** [missing-regression-guard] The non-blocking property of the
   unflagged-work-skill warning exists only in prose; all four denylistGate
   pinned tests cover blocking paths. A refactor folding the warning into
   the refusal path silently restores the round-3 defect (sync bricked by
   uninstalling a work skill) and `npm test` stays green.
   *Concrete fix:* pinned test #19 covering warning-not-refusal and
   absent-slug-no-warning.

### Planner decisions (Claude)

1. **Accept (with local verification)** — planner ran `tmutil
   destinationinfo`: NO Time Machine destination configured on this
   machine, confirming the premise was false today. Fix applied: Task 3
   gains a hard prerequisite (reference copy of the term list in an
   encrypted password-manager secure note — or equivalent non-repo
   encrypted store; dotfiles repos explicitly banned; mechanism NAME
   recorded in CLAUDE.md, terms never); both prose claims rewritten to
   reference the named mechanism; Verification list gains the
   recovery-mechanism check.
2. **Accept** — pinned test #19 added verbatim.

### Plan edits applied

- Global constraints: "covered by home-dir backups" claims replaced by the
  named-mechanism design, with the tmutil verification recorded.
- Task 3: recovery-mechanism setup as a hard prerequisite of first --write.
- Task 2: pinned test #19.
- Verification: denylist recovery-mechanism line.
- Status line: rounds 1–5 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 2 (high=0, medium=1, low=1) + 0 open questions
- Decisions: 2 Accept, 0 Reject, 0 Uncertain
- Cumulative: 28 findings raised, 28 accepted, 0 rejected
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L → 1M+1L

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r5/result.md`.

## Round 6 — 2026-08-16T19:15-07:00

2 findings (1 medium, 1 low), zero open questions.

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [unowned-build-input] Showcase images are a required build
   input with rendering design but no lifecycle: no task produces them, no
   rule defines missing-file behavior (both static import and the research
   doc's import.meta.glob approach fail the build on a missing file), no
   verification covers them. Bites first at Task 5 (project cards specify
   screenshots; a placeholder image would reintroduce the mockup-only-
   artifact class rounds 4–5 eliminated) and then at Task 6 (build failure
   on the autonomous critical path).
   *Concrete fix:* image collection owned in Task 4 (before the mockup);
   mergeCatalog drops unresolvable images with a named build warning;
   real-data verification line.

2. **[LOW]** [storage-lifecycle-gap] The term-list reference copy is
   verified once at setup but nothing keeps it true: an edit to the working
   file silently diverges from the secure note, and a later rebuild
   restores the stale copy — reopening round 5's gap one edit later. Also
   `~/.config` is the most conventional dotfiles root; the dotfiles ban
   covered the reference copy but not the working file's own location
   (verified: no dotfiles repo exists today, so latent).
   *Concrete fix:* CLAUDE.md gotchas — same-sitting reference-copy update
   (with a local uncommitted `.last-sync` term-count reminder from sync)
   and never bring `~/.config/skill-shelf/` under version control.

### Planner decisions (Claude)

1. **Accept** — decision #4 softened (imagery optional per entry,
   text-only cards legal); Task 4 owns image collection before the mockup
   (web-sized < 500 KB, hand-picked by Zehui); mergeCatalog drops
   unresolvable `images:` entries with a warning naming slug + filename;
   unit fixture test + real-data resolution test added; Verification line
   added.
2. **Accept** — both gotcha lines adopted; sync maintains
   `~/.config/skill-shelf/.last-sync` (local, uncommitted) and prints the
   update-reference-copy reminder on term-count change.

### Plan edits applied

- Decision #4: screenshots-when-available wording.
- Task 4: image collection step, missing-image degradation, two new tests.
- Task 3: `.last-sync` reminder behavior.
- Verification: denylist lifecycle gotchas + images-resolve line.
- Status line: rounds 1–6 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 2 (high=0, medium=1, low=1) + 0 open questions
- Decisions: 2 Accept, 0 Reject, 0 Uncertain
- Cumulative: 30 findings raised, 30 accepted, 0 rejected
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L → 1M+1L → 1M+1L

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r6/result.md`.

## Round 7 — 2026-08-16T19:45-07:00

2 findings (1 medium, 1 low), zero open questions — both defects in the
round-6 resolutions, self-flagged by the reviewer.

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [unenforced-sequencing] "Zehui hand-picks images" is a
   human action embedded mid-Task-4 with no stop marker, inside a pipeline
   authorized to run autonomously to `git push`; with imagery now optional,
   the safest autonomous behavior is to collect nothing and proceed — the
   real-data images test and the Verification line are both satisfied
   vacuously by the empty set, and Task 5's deliverable never mentioned
   images. Result: mockup approved without imagery, images arrive after the
   design gate is spent — the exact ordering the fix was written to prevent.
   *Concrete fix:* move the imagery choice INTO the existing Task 5 gate;
   Task 4 keeps only implementer actions; Task 8 launch requirement makes
   zero-images a recorded decision.

2. **[LOW]** [weak-change-detection] `.last-sync` stores a term COUNT, so
   same-count edits (spelling fix, term replacement) silently diverge the
   working list from the reference copy; and the plan never says which mode
   writes the file — a dry-run write would consume the reminder before the
   change is applied. The reviewer noted the count was its own round-6
   suggestion, and that the round-4 oracle objection doesn't apply to a
   local uncommitted hash.
   *Concrete fix:* store sha256 of the sorted term list; write only on
   `--write`.

### Planner decisions (Claude)

1. **Accept** — Task 4 reduced to implementer-only groundwork (create
   `src/showcase/`, no `images:` keys unless the file exists); Task 5 now
   owns the imagery choice inside its iterate-until-approved loop (mockup
   regenerated with supplied images); Task 8 launch requirements gain the
   recorded imagery decision.
2. **Accept** — `.last-sync` holds the sha256 (local, uncommitted, no
   oracle); written only on `--write` so the reminder persists across
   dry-runs.

### Plan edits applied

- Task 4: image step rewritten as implementer groundwork.
- Task 5: thumbnails rendered; imagery chosen and iterated inside the gate.
- Task 8: imagery-decision launch requirement.
- Task 3: `.last-sync` content and write-timing pinned.
- Status line: rounds 1–7 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 2 (high=0, medium=1, low=1) + 0 open questions
- Decisions: 2 Accept, 0 Reject, 0 Uncertain
- Cumulative: 32 findings raised, 32 accepted, 0 rejected
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L → 1M+1L → 1M+1L → 1M+1L

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r7/result.md`.

## Round 8 — 2026-08-16T20:15-07:00

2 findings (1 medium, 1 low), zero open questions.

### Reviewer findings (Claude Opus 5 pane)

1. **[MEDIUM]** [unreviewed-binary-input] Rounds 6–7 opened a new content
   channel into a confirmed-PUBLIC repo — human-supplied screenshots — and
   every org-leak layer is text-only: slugs, entry fields, JSON diffs, and
   grep cannot see text rendered as pixels. The highest-risk image is the
   one the plan requires: a herdr screenshot naturally shows pane titles,
   cwd, repo/branch names of live agent sessions. Committed images also
   carry metadata (tooling, device, username). Every automated gate passes
   cleanly on a leaky screenshot; a committed binary survives in git
   history after deletion and no future grep sweep will surface it.
   *Concrete fix:* per-image disclosure review before the `images:` key is
   added and before commit; metadata strip (`exiftool -all=`); Task 8
   launch requirement extended to cover image content.

2. **[LOW]** [vacuous-at-authoring-time] The real-data images test is
   authored in Task 4, where round 7's fix guarantees there are no
   `images:` keys — vacuous pass — and Task 5 (where the keys appear)
   never re-runs `npm test`; its first meaningful run is CI at Task 7. A
   filename typo therefore degrades to a silently thumbnail-less mockup
   inside the very gate whose purpose is choosing imagery.
   *Concrete fix:* Task 5 runs `npm test` after each mockup regeneration;
   Task 4 notes the expected vacuous pass.

### Planner decisions (Claude)

1. **Accept** — Task 5 gains the full disclosure-review protocol (terminal
   /editor content, titles, banners, tabs; tight-crop preference; herdr
   flagged as highest-risk; review BEFORE key-add and BEFORE commit since
   git history preserves originals) plus metadata stripping; Task 8 launch
   requirement extended to "every committed image reviewed and stripped".
2. **Accept** — Task 5 re-runs `npm test` inside the gate; Task 4 records
   the vacuous-pass expectation so green ≠ coverage confusion.

### Plan edits applied

- Task 5: disclosure review + exiftool strip + npm test inside the gate.
- Task 4: vacuous-pass annotation on the images test.
- Task 8: image-content launch requirement.
- Status line: rounds 1–8 applied.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 2 (high=0, medium=1, low=1) + 0 open questions
- Decisions: 2 Accept, 0 Reject, 0 Uncertain
- Cumulative: 34 findings raised, 34 accepted, 0 rejected
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L → 1M+1L → 1M+1L → 1M+1L → 1M+1L

### Reviewer raw response

Not embedded (same redaction rationale). Local artifact:
`~/.local/state/herdr-orch/ss-plan-review-r8/result.md`.

## Round 9 — 2026-08-16T20:50-07:00 (FINAL — round ceiling set by user)

1 finding (low), zero open questions.

### Reviewer findings (Claude Opus 5 pane)

1. **[LOW]** [control-not-durable] The round-8 image-disclosure protocol is
   consumed entirely at launch: it exists only in Task 5's gate and Task
   8's launch requirement, while Task 8's CLAUDE.md update list has no
   imagery entry — so post-launch screenshots (which the plan anticipates)
   skip the review, displacing the round-8 risk in time. Also
   `exiftool -all=` is specified but not installed on this machine (nor is
   ImageMagick); no fallback named.
   *Concrete fix:* imagery gotcha added to the CLAUDE.md list Task 8
   already edits; name a verified-available strip fallback (`sips`, macOS
   built-in).

### Planner decisions (Claude)

1. **Accept** — two-line fix inside edits Task 8 already makes; not
   edge-case bloat. Planner verified tool availability: `exiftool`,
   `magick`, `mogrify` all absent; `/usr/bin/sips` present. Task 5's strip
   step now names `sips -s format png` as the verified mechanism; Task 8's
   CLAUDE.md list gains the standing imagery gotcha.

### Plan edits applied

- Task 5: metadata strip re-specified around the verified-available tool.
- Task 8: imagery gotcha added to the CLAUDE.md update list.
- Status line: REVIEWED.

### Round stats

- Reviewer: Claude Opus 5 (herdr pane, xhigh; subscription — no per-token cost)
- Findings: 1 (high=0, medium=0, low=1) + 0 open questions
- Decisions: 1 Accept, 0 Reject, 0 Uncertain

## End report

- **Final status: user-capped (round ceiling 9 set by Zehui), exiting on a
  single low-severity finding — accepted and applied.** No unresolved
  findings, no unresolved open questions, no deferred items. Zehui
  explicitly accepted residual risk from stopping without a further
  verification round ("我接受一定的risk，不需要过多的边界条件").
- Rounds run: 9
- Findings: 35 raised, 35 accepted (several with planner modifications —
  notably the round-1 denylist storage and round-4 fingerprint removal),
  0 rejected, 0 deferred. Open questions: 6 raised, 6 resolved (4 by the
  user via AskUserQuestion, 2 by planner fact-lookup).
- Severity histogram across the run: high=6, medium=21, low=8.
- Severity trend: 3H+5M+1L → 3H+7M → 3M+1L → 2M+1L → 1M+1L ×4 → 1L.
- Cost: $0 API (reviewer ran as a Claude Opus 5 herdr pane on
  subscription).
- Final plan: `plans/v0.1.0-skill-shelf.md`
- This log: `plans/fixs/v0.1.0-skill-shelf-fixes.md` (rendered view;
  unredacted round artifacts live locally under
  `~/.local/state/herdr-orch/ss-plan-review-r*/`, never committed).
